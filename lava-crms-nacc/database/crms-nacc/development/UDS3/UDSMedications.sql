-- no table changes

-- no metadata needed because all UDS 2 metadata can be used as there are no form changes
-- and the custom UdsMedications2Handler loads the list data for udsmedications2 metadata 
-- properties
